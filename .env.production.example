# Production Environment Variables for Azure App Service
# Set these in Azure Portal → App Service → Configuration → Application settings

PORT=8080
NODE_ENV=production

# MongoDB Connection - MongoDB Atlas (cloud)
# Add this exact connection string with URL-encoded password
# ! = %21, @ = %40
MONGODB_URI=mongodb+srv://dbadmin_user:%21Obenyathi%402025@cluster0.ck9sh20.mongodb.net/cemetery_management?appName=Cluster0

# JWT Secret - Use a strong secret for production
JWT_SECRET=cemetery-management-production-secret-key-2025-change-this
